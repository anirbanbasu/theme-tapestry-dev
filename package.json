{
  "name": "tapestry-build-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Build-time tooling for the Tapestry Zola theme demo site (e.g. KaTeX pre-rendering). Not published; scoped to this repo's build pipeline only.",
  "scripts": {
    "prebuild": "node scripts/render-katex.js",
    "build": "npm run prebuild && zola --root .katex-build/site build",
    "serve": "npm run prebuild && zola --root .katex-build/site serve"
  },
  "dependencies": {
    "katex": "^0.17.0"
  }
}
