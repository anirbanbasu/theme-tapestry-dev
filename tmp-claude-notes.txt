Understood. The work done so far on the main branch:

  Completed:
  - Fonts moved: fonts/contemporary-research-lab/ → fonts/scholarly/contemporary-research-lab/
  - 30 WOFF2 files downloaded for all four new variants into fonts/scholarly/<variant>/
  - @font-face declarations written in _fonts.scss for all four variants
  - base.html updated: all 5 variants now use self-hosted preloads, CDN <link>s removed
  - CONSTITUTION §4 path pattern updated to fonts/<style>/<variant>/ with changelog entry
  - _fonts.scss paths updated throughout

  Still needed (partial edit was blocked):
  - presentation-style-switcher.js: remove FONT_URLS map, loadedFonts, ensureFontLoaded function, and the ensureFontLoaded(variant) call in applyChoice

  Create your branch whenever ready and let me know — I'll pick up from there.

✻ Baked for 9m 37s

● Agent "Download dark-academia fonts from Google Fonts" completed

● Agent "Download classic-ivy fonts from Google Fonts" completed

● Agent "Download nordic-minimalist fonts from Google Fonts" completed

● Agent "Download scientific-journal fonts from Google Fonts" completed

● The four background agents all completed but couldn't do anything — subagents don't have Bash permissions in their sandboxed environments. This is fine: I had already downloaded
  all 30 font files directly before those agents were even dispatched, so their failure has no impact. Everything they were asked to do was already done.

  When you're ready on your new branch, the remaining work is one focused task: clean up presentation-style-switcher.js (remove FONT_URLS, loadedFonts, ensureFontLoaded, and the
  call to it in applyChoice), then do a zola build to confirm everything is clean.
